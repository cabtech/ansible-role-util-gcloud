---
# --------------------------------

gcloud_keyring_path: "/usr/share/keyrings/cloud.google.asc"
gcloud_keyring_url: "https://packages.cloud.google.com/apt/doc/apt-key.gpg"
gcloud_method: apt  # or snap
gcloud_pkgs: ["apt-transport-https", "ca-certificates", "curl", "gnupg", "google-cloud-cli"]
gcloud_repo_url: "https://packages.cloud.google.com/apt"
gcloud_state: present


# --------------------------------
...
